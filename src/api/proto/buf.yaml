version: v1

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - STANDARD
    - COMMENT_ENUM
    - COMMENT_ENUM_VALUE
    - COMMENT_FIELD
    - COMMENT_MESSAGE
    - COMMENT_ONEOF
    - COMMENT_RPC
    - COMMENT_SERVICE
  except:
    - PACKAGE_DIRECTORY_MATCH

breaking:
  use:
    - FILE
